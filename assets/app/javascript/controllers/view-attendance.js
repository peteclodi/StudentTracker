angular.module("studentTrackerApp").controller('ViewAttendanceCtrl', ['$scope', '$http',
    function ($scope, $http) {
        // Get Students listing from the data server
//        $http.get('phones/phones.json').success(function(data) {
//            $scope.phones = data;
//        });
    }]);
